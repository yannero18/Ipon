<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!--
        Section 4 reserves Jeepney Orange specifically for action triggers
        (the transaction coin FAB), not general branding surfaces. The app
        icon represents the whole app, not a single action, so it uses
        Ocean Teal, "structural calm, trust, anchored stability," as
        the background instead, matching how the color is actually used
        everywhere else in the app (balance card, nav selection state).
    -->
    <path
        android:fillColor="#1D5D6B"
        android:pathData="M0,0h108v108h-108z" />
</vector>
